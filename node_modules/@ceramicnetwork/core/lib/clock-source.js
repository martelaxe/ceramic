"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ClockSource = void 0;
class ClockSource {
    now() {
        return new Date();
    }
    async waitUntil(until) {
        const delay = async function (ms) {
            return new Promise((resolve) => setTimeout(() => resolve(), ms));
        };
        let now = this.now();
        while (now < until) {
            await delay(until.getTime() - now.getTime());
            now = this.now();
        }
    }
}
exports.ClockSource = ClockSource;
//# sourceMappingURL=clock-source.js.map