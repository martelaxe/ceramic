import { Observable, Subscription } from 'rxjs';
import { PubsubMessage } from './pubsub-message';
import { DiagnosticsLogger } from '@ceramicnetwork/common';
import { ObservableWithNext } from './observable-with-next';
export declare class PubsubRateLimit extends Observable<PubsubMessage> implements ObservableWithNext<PubsubMessage> {
    private readonly pubsub;
    private readonly logger;
    private readonly queriesPerSecond;
    private readonly _recentQueries;
    private readonly _queryQueue;
    private readonly _clock;
    constructor(pubsub: ObservableWithNext<PubsubMessage>, logger: DiagnosticsLogger, queriesPerSecond: number);
    next(message: PubsubMessage): Subscription;
    private _publishQuery;
}
//# sourceMappingURL=pubsub-ratelimit.d.ts.map